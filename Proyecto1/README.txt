Posible ejemplo:
ROBOT_R
VARS a , b
BEGIN
{assign (a ,3) ;
assign (b ,1) ;}
turn( right ) ;
[move(3);
moveInDir(2 , south );]
face( east) ;
skip();
moveDir(1 , left );
END

No sirve en todos los casos, toca, verificar los bloques. Se pueden hacer mas ejemplos que funcionen. Recuerden la sintaxis, como que moveInDir con I mayuscula. Funciona con espacios, tabs o enters, con corchetes. No olvidar el ; despues de cada accion.

Nombres:
Jose Daniel Fuentes.		201631094
Laura Rodriguez Rodriguez. 	201816069